version: '3'

services:
  web:
    image: :web_latest
    build: .
    container_name: webglimpse
    command: bash -c "python manage.py makemigrations && python manage.py migrate && gunicorn glimpseAPI.wsgi -b 0.0.0.0:8000"
    ports:
      - "8000:8000"
    volumes:
      - ./src:/src
      - ./config/nginx:/etc/nginx/conf.d
    expose:
      - "8000"